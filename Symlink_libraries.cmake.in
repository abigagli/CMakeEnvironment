EXECUTE_PROCESS (COMMAND "@CMAKE_CXX_COMPILER@" "-print-file-name=libstdc++.so" OUTPUT_VARIABLE LIBSTDC_PATH)
EXECUTE_PROCESS (COMMAND "@CMAKE_C_COMPILER@" "-print-file-name=libgcc_s.so" OUTPUT_VARIABLE LIBGCC_PATH)
MESSAGE (STATUS "Should link to ${LIBSTDC_PATH} and ${LIBGCC_PATH}")
